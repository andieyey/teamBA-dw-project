version: 2


models:
  - name: monthly_category_revenue
    columns:
      - name: sale_month
        tests:
        - not_null
      - name: category_name
        tests:
        - not_null


  - name: top_customers
    columns:
      - name: customer_id
        tests:
          - not_null